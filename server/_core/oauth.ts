import { COOKIE_NAME, ONE_YEAR_MS } from "@shared/const";
import type { Express, Request, Response } from "express";
import * as db from "../db";
import { getSessionCookieOptions } from "./cookies";
import { sdk } from "./sdk";

function getQueryParam(req: Request, key: string): string | undefined {
  const value = req.query[key];
  return typeof value === "string" ? value : undefined;
}

export function registerOAuthRoutes(app: Express) {
  app.get("/api/oauth/callback", async (req: Request, res: Response) => {
    const code = getQueryParam(req, "code");
    const state = getQueryParam(req, "state");

    if (!code || !state) {
      res.status(400).json({ error: "code and state are required" });
      return;
    }

    try {
      const tokenResponse = await sdk.exchangeCodeForToken(code, state);
      const userInfo = await sdk.getUserInfo(tokenResponse.accessToken);

      if (!userInfo.openId) {
        res.status(400).json({ error: "openId missing from user info" });
        return;
      }

      await db.upsertUser({
        openId: userInfo.openId,
        name: userInfo.name || null,
        email: userInfo.email ?? null,
        loginMethod: userInfo.loginMethod ?? userInfo.platform ?? null,
        lastSignedIn: new Date(),
      });

      const sessionToken = await sdk.createSessionToken(userInfo.openId, {
        name: userInfo.name || "",
        expiresInMs: ONE_YEAR_MS,
      });

      const cookieOptions = getSessionCookieOptions(req);
      res.cookie(COOKIE_NAME, sessionToken, { ...cookieOptions, maxAge: ONE_YEAR_MS });

      // Decode returnPath from state if present (format: "redirectUri|/returnPath")
      let postLoginPath = "/dashboard/home";
      try {
        console.log("[OAuth] Raw state received:", state);
        const decoded = Buffer.from(state, "base64").toString("utf-8");
        console.log("[OAuth] Decoded state:", decoded);
        const parts = decoded.split("|");
        console.log("[OAuth] State parts count:", parts.length, "part[1]:", parts[1]);
        if (parts.length >= 2 && parts[1].startsWith("/")) {
          postLoginPath = parts[1];
        }
        console.log("[OAuth] Redirecting to:", postLoginPath);
      } catch (e) {
        console.log("[OAuth] State decode error:", String(e));
        // ignore decode errors, fall back to /
      }
      res.redirect(302, postLoginPath);
    } catch (error) {
      console.error("[OAuth] Callback failed", error);
      res.status(500).json({ error: "OAuth callback failed" });
    }
  });
}
